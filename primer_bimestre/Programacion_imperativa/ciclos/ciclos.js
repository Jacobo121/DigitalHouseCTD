/* function loro(texto) {
    for(let i = 0; i < 5; i++) {
        console.log(texto)
    }
}

loro("hola") */

/* function noParesDeContarImparesHasta(numero) {
    let count = 0;
    for(let i = 0; i <= numero; i++) {
        let impar = i % 2;
        if(impar == 1) {
            count++
        } 
    }
    console.log(count);
}

console.log(noParesDeContarImparesHasta(5)) */
/* let numeros = [7,8,4,3,6,5,4]

function encontroUn5(array) {
    let count = 0
    do {
        if(array[count] == 5) {
            var texto = "se encontró un 5!"
        }
        count++
    } while(count < array.length)
    return texto
}

console.log(encontroUn5(numeros)) */






/* let numeros = [5, 2, 3, 4, 0, 6, 7, 8, 9, 5];

function encontreUn5(numeros) {
  let index = 0;

  do {
    console.log(numeros[index]);
    if(numeros[index] == 5) {
    console.log("Se encontró un 5!")
    }
    index++
  } while (numeros[index] != 5);
}

encontreUn5(numeros) */



/* let numero = [5,8,4,3,6,5,4]

function encontroUn5(numeros) {
    let count = 0
    do {
        console.log(numeros[count])
        count++
        if(numeros[count] == 5) {
            console.log( "se encontró un 5!")
        }
    } while(numeros[count] != 5)
}

encontroUn5(numero) */